using System;

class MarketingFirmEmployee
{

    /// <summary>
    /// A marketing firm wants to keep record of its employees. Each record would have the 
    /// following characteristics – first name, family name, age, gender (m or f), ID number, 
    /// unique employee number (27560000 to 27569999). 
    /// Declare the variables needed to keep the information for a single employee using appropriate data types and descriptive names.
    /// </summary>
    static void Main()
    {
        string firstName;
        string familyName;
        byte age;
        char gender;
        int employeeID;

        //example of usage
        firstName = "Gosho";
        familyName = "Peshov";
        age = 20;
        gender = 'm';
        employeeID = 27569999;

    }
}